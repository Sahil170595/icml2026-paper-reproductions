# GPU kit for Claim 6 (ViT: Tucker vs GQA vs MLA on an ImageNet subset).
# torch is provided by the CUDA base image on Hugging Face Jobs; the rest install
# automatically via `hf jobs uv run` (PEP-723 inline deps in run.py) or with
# `pip install -r requirements.txt` inside a pytorch image.
torch>=2.1
timm>=1.0.0
datasets>=2.18
torchvision>=0.16
pillow>=9.0
