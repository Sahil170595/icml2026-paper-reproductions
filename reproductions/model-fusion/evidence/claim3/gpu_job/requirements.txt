# GPU-job deps for run.py (torch + CUDA are provided by the --image).
# Mirrors the official repo pyproject.toml (commit e84a687...) so the pinned
# fusion code runs unchanged; run.py also does `pip install -e <repo>`.
huggingface_hub>=0.24
captum~=0.7.0
datasets~=3.5.1
medmnist==3.0.2
scikit-learn~=1.6.0
scipy~=1.13.1
timm~=1.0.19
torchvision~=0.23.0
torchmetrics>=1.8.2
torchsummary~=1.5.1
pot~=0.9.5
numpy~=2.0.2
pandas~=2.2.3
h5py>=3.15.1
tqdm~=4.67.1
